import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: "#1F3A5F", // your primary color
      },
      secondary: {
        main: "#F97316", // your secondary color
      },
    },
  });

  export default theme;
